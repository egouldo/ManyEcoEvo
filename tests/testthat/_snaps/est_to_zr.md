# est_to_zr emits cli warning for missing inputs

    Code
      est_to_zr(beta_estimate = NA, beta_SE = 1, adjusted_df = 10)
    Message
      x Required values for computing standardised effect sizes missing:
      ! Returning "NA" for tupple:
      1. beta_estimate NA,
      2. beta_se 1,
      3. adjusted_df 10.
    Output
      [1] NA

